"""Context-building modules for gathering release metadata.

These modules fetch data from external sources (GitHub, CI systems,
incident databases) and assemble it into a structured format the
agent can reason about.
"""
